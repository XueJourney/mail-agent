{
  "name": "mail-agent",
  "version": "1.0.0",
  "description": "A self-hosted mail agent that syncs IMAP accounts and receives emails via Cloudflare Email Workers webhook, storing everything in SQLite with a simple REST API.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "keywords": ["imap", "email", "self-hosted", "cloudflare", "sqlite"],
  "author": "XueJourney",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "dotenv": "^17.3.1",
    "imapflow": "^1.2.10",
    "mailparser": "^3.9.3"
  }
}
